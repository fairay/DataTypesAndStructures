#include "head.h"
/*
int create_array(array_t *a, size_t n)
{
    (*a) = (array_t)calloc(n, sizeof(int));
    if (*a)
        return OK;
    else
        return ALLOC_FAIL;
}

void print_array(array_t a, size_t n)
{
    for (int i = 0; i < n; i++)
       printf("%6d ", a[i]);
    printf("\n");
}

void delete_array(array_t *a)
{
    free(*a);
    (*a) = NULL;
}

// Applied functions
int resize_array(array_t *a, int n)
{
    array_t new_arr = NULL;
    new_arr = (array_t)realloc((*a), n * sizeof(int));
    if (new_arr)
    {
        (*a) = new_arr;
        new_arr = NULL;
        return OK;
    }
    else
        return ALLOC_FAIL;
}



int create_str_arr(str_arr_t* sarr)
{

}
*/

